name: Terraform

on:
  push:
    branches: [master]
    paths: ['terraform/**']
  pull_request:
    branches: [master]
    paths: ['terraform/**']
  workflow_dispatch:

concurrency:
  group: terraform-xomtracks-infrastructure
  cancel-in-progress: false

permissions:
  contents: read
  pull-requests: write

env:
  TF_DIR: terraform
  AWS_REGION: us-east-1

jobs:
  terraform:
    name: Terraform
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ${{ env.TF_DIR }}

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v4
        with:
          terraform_version: "1.5.7"

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v6
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Terraform Init
        run: terraform init

      - name: Terraform Validate
        run: terraform validate

      - name: Terraform Plan
        id: plan
        # Tee to disk so the comment step can read it without stuffing the
        # whole plan into argv/env (Linux ARG_MAX = 128KB combined; large
        # plans blow past that).
        #
        # No TF_VAR_* secrets needed -- an earlier version of this
        # workflow wired 5 app secrets via TF_VAR_XOMTRACKS_* GitHub
        # secrets that were never set, which is what caused the first real
        # apply to fail (empty string passed Terraform's own validation
        # but AWS SSM's PutParameter API rejects an empty Value). Fixed at
        # the Terraform layer instead (see ssm.tf/random.tf) so this
        # workflow no longer depends on any repo secret beyond AWS creds.
        run: terraform plan -no-color -input=false -lock=false 2>&1 | tee /tmp/tf-plan.txt
        continue-on-error: true

      - name: Comment Plan on PR
        uses: actions/github-script@v7
        if: github.event_name == 'pull_request'
        with:
          script: |
            const fs = require('fs');
            const raw = fs.existsSync('/tmp/tf-plan.txt')
              ? fs.readFileSync('/tmp/tf-plan.txt', 'utf8')
              : 'No output';
            const truncated = raw.length > 60000
              ? raw.substring(0, 60000) + '\n... truncated'
              : raw;
            const body = '## Terraform Plan\n```\n' + truncated + '\n```';
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body
            });

      - name: Terraform Apply
        if: github.ref == 'refs/heads/master' && github.event_name == 'push'
        run: terraform apply -auto-approve -input=false
